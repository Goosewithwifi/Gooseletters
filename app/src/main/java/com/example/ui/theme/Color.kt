package com.example.ui.theme

import androidx.compose.ui.graphics.Color

val EditorialBlack = Color(0xFF2B2927) // Soft, friendly charcoal instead of stark pure black
val EditorialWhite = Color(0xFFFFFFFF)
val EditorialBackground = Color(0xFFFAF6F0) // Warm, creamy off-white/milky background
val EditorialMuted = Color(0xFF8D867E) // Soft taupe for secondary/muted text
val EditorialBorder = Color(0xFFEFE9DF) // Very soft, creamy outline borders instead of harsh gray
val EditorialBurgundy = Color(0xFFDF633D) // Friendly terracotta/coral accent for highlights

val MilkyOrange = Color(0xFFE88A1A) // Warm pastel orange for highlights/categories
val MilkyCardBg = Color(0xFFFFFFFF) // Crisp milk-white card background
val MilkyShadow = Color(0x0C000000) // Soft, diffused low-opacity shadow color
